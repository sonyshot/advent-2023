use std::fs::read_to_string;

pub fn solve() -> u32 {
    let filepath = "assets/puzzle2/input.txt";
    let mut sum: u32 = 0;

    read_to_string(filepath).unwrap().lines().filter(||);

}

fn possible_game(file_line: &str) -> bool {
    
}
